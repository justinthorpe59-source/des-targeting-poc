// Placeholder — real filterable population list lands at M4. Matches the
// same "not built yet" pattern already used for System2Root.
export function Population() {
  return (
    <section className="space-y-2">
      <h1 className="text-lg font-semibold">Population</h1>
      <p className="max-w-md text-sm text-slate-600">
        Not built yet. Milestone M4 adds the filterable list (division / team / location).
      </p>
    </section>
  )
}
